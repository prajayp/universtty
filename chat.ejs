<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Query Chat</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #a8d8d8; /* Teal background */
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      max-width: 500px;
      width: 90%; /* Ensure responsiveness */
      background: #ffffff; /* White content box */
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    h1 {
      color: #333; /* Dark text */
      margin-bottom: 20px;
    }

    form {
      margin-top: 10px;
    }

    input[type="text"] {
      width: calc(100% - 20px);
      padding: 10px;
      margin-bottom: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      background-color: #ffa31a; /* Orange button */
      color: white;
      padding: 10px 20px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #e69500; /* Darker orange */
    }

    .result {
      margin-top: 20px;
      background: #f9f9f9; /* Light grey for results */
      padding: 15px;
      border-radius: 5px;
      border: 1px solid #ddd;
      max-height: 200px;
      overflow-y: auto;
    }

    .error {
      color: red;
      margin-top: 10px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Query Chat</h1>
    <form action="/chat" method="POST">
      <input type="text" name="query" placeholder="Enter your query..." required>
      <button type="submit">Submit</button>
    </form>

    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>

    <% if (queryResult && queryResult.length > 0) { %>
      <div class="result">
        <h3>Query Result:</h3>
        <pre><%= JSON.stringify(queryResult, null, 2) %></pre>
      </div>
    <% } else if (queryResult) { %>
      <div class="result">
        <h3>No Results Found</h3>
      </div>
    <% } %>
  </div>
</body>
</html>